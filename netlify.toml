[build]
  publish = "public"
  functions = "netlify/functions"

# Map /api/<name> -> /.netlify/functions/<name> so the client can keep calling /api/*
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

